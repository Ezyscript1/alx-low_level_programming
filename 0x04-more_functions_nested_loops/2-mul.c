#include "main.h"

/**
 * mul - to multiply two ints
 * @a: the int to be multiplies
 * @b: the second to be multiplied
 * Retun: Always 0
 */

int mul(int a, int b)
{
	int ret;
	
	ret = a * b;

	return (ret);
}
